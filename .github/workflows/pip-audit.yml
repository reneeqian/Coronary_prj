name: pip-audit

on:
  push:
    branches: [main, dev]
  pull_request:
    branches: [dev, main]
  schedule:
    - cron: "0 6 * * 1"

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

permissions:
  contents: read

jobs:
  pip-audit:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pip-audit pyyaml numpy torch matplotlib openpyxl pydicom scikit-image
          pip install "forge-utils @ git+https://github.com/reneeqian/forge.git@v0.1.0"
          pip install "git+https://github.com/reneeqian/regulatory_tools.git@v1.0.0"
          pip install "git+https://github.com/reneeqian/medical_image_ai_toolkit.git@dev"
          pip install -e .

      - name: Run pip-audit
        run: pip-audit

      - name: Generate SBOM
        run: pip-audit --format cyclonedx-json --output sbom.json

      - name: Upload SBOM artifact
        uses: actions/upload-artifact@v4
        with:
          name: sbom
          path: sbom.json
