[pytest]
testpaths =
    tests

pythonpath =
    src

markers =
    requirement(req_id): links a test to one or more requirement IDs

markers =
    requires_dataset: integration tests that require external datasets